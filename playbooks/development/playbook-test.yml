---
- name: Test playbook
  hosts: localhost
  gather_facts: false
  vars_files:
    - cobra.vault.yml
  tasks:
    - name: Debug vault variables
      ansible.builtin.debug:
        var: item.value
      loop: "{{ lookup('dict', vars) }}"
